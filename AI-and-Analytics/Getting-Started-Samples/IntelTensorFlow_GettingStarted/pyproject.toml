[project]
name = "intelTensorFlow_gettingstarted"
version = "0.1.0"
description = "This sample illustrates how to train a TensorFlow model and run inference with oneMKL and oneDNN."
authors = [
    {name = "Copyright © 2023 Intel Corporation"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "intel-extension-for-tensorflow[cpu]==2.15",
]

[dependency-groups]
dev = [
    "ipykernel>=6.29.5",
]
